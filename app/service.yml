apiVersion: v1
kind: Service
metadata:
  name: project-service
  namespace: project-dev
spec:
  type: NodePort
  selector:
    app: project
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
